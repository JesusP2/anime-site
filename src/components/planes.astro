---

---

<div class="top-[15rem] plane-container plane" />
<div class="top-[15rem] plane-container plane" />
<div class="top-[15rem] plane-container plane" />
<div class="top-[15rem] plane-container plane" />
<div class="top-[15rem] plane-container plane" />
<script>
  import { createPlaneAnimations } from "@/lib/plane-animations";
  const plane = document.querySelectorAll(".plane");
  plane.forEach((plane) => {
    const direction = ['left', 'right'][Math.floor(Math.random() * 2)]
    createPlaneAnimations(plane, direction);
  });
</script>
<style>
  .plane-container {
    position: absolute;
    background-size: contain;
    background-repeat: no-repeat;
  }
</style>
